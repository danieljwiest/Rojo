import "./ScoreMarker.styles.css";

const ScoreMarker = () => {
  return (
    <div className={`scoreMarkerBase`}>
      <div className={`scoreMarker`}></div>
    </div>
  );
};

export default ScoreMarker;
